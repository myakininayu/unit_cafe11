import Image from 'react-bootstrap/Image';
import Table from 'react-bootstrap/Table';

function Workers() {
  return (
    <Table striped bordered hover>
        {
          // На Workers ничего не надо делать, только CRUD на продукты
        }   
    </Table>
  );
}

export default Workers;
